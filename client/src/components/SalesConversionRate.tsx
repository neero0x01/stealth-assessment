import React from 'react';

export const SalesConversionRate = (props: any) => {
  return (
    <div
      style={{
        height: '500px',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      {' '}
      <h2 style={{ fontSize: '110px' }}>67%</h2>
    </div>
  );
};
